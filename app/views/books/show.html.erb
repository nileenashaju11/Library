<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @book.content %>
</p>
<p>
  <strong>Author:</strong>
  <%= @book.author.name %>
</p>
<%= render partial: "pitcures/pitcure", locals: {imagable: @book}%>
<%= render partial: "pitcures/form", locals: {imagable: @book}%>
<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>
